# Environment configuration for Student Mental Health Predictor API

# Server settings
HOST=0.0.0.0
PORT=8000

# Path to the trained ML model pipeline (relative or absolute)
MODEL_PATH=../ml/models/Mental_Health_model.pkl
MODEL_NAME=Mental_Health_model.pkl

# CORS Origins (comma separated or JSON array if needed)
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
